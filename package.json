{
    "name": "testapi",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
      "crud-app"
    ],
    "engines": {
      "node": "18.x"
    },
    "scripts": {
      "heroku-postbuild": "cd crud-app && npm install && npm run build --prod"
    },
    "devDependencies": {
      "@angular/cli": "^18.0.0"
    }
  }