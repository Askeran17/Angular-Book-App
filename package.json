{
  "name": "testapi",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "ngx-cookie-service": "^19.0.0"
  },
  "scripts": {
    "heroku-postbuild": "cd crud-app && npm install && npm run build --prod"
  }
}